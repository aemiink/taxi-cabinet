{
  "name": "personal-portfolio",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "npm run build && cd server && npm install && node server.js"
  }

}
