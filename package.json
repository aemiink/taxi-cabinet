{
  "name": "personal-portfolio",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build:client",
    "build:client": "cd client && npm ci --legacy-peer-deps && npm run build",
    "start": "cd server && npm ci --omit=dev --legacy-peer-deps && node server.js"
  }
}
